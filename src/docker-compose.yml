services:

  catalogdb:
    image: postgres

  catalog.api:
    image: ${DOCKER_REGISTRY-}catalogapi
    build:
      context: ..
      dockerfile: src/services/Catalog/Catalog.API/Dockerfile

volumes:
  postgres_catalog:

